# TODOコメントを放置しない

## ねらい

TODO/FIXME を放置すると、いつまでも着手されず「負債の見える化」ではなく「負債の放置」になる。
残すなら、**いつ/誰が/何を** するのかを追跡可能にする。

## チェック観点

- TODO/FIXME が **放置** されていないか（期限/優先度/追跡先なし）
- 残す必要があるなら、Issue 化などで **追跡可能** になっているか
- TODO が「仕様の穴」や「設計判断の未完」を示す場合、DR/ドキュメントで補うべきではないか

## 指摘する基準（境界）

- 追跡先（Issue/PR/DR）も期限/優先度も無い TODO/FIXME は「放置」とみなし、原則指摘する
- TODO が「後で直す」以上の意味（仕様未決・設計の保留）を含む場合は、**ドキュメント化（DR）** も提案する

## 例

### 良い例（次アクションが追跡できる）

```java
public void calculateDiscount(Order order) {
    // TODO: 複雑なロジックの最適化が必要
    // - 次のスプリントで対応
    // - Issue #13 としてチケット化済
    if (order.isPremium()) {
        order.setDiscount(20);
    } else {
        order.setDiscount(10);
    }
}
```

## 補足（推奨運用）

- TODO を残すなら、最低限「理由」「いつ対処するか」「追跡先（Issue/PR/DR）」のどれか（可能なら全部）を付ける
- TODO が増え続ける箇所は、スコープを切って「負債返済 PR」を作る判断もする
